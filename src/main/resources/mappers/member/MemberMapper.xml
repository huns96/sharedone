<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sharedOne.mapper.MemberMapper">
    <select id="memberList" resultType="com.sharedOne.domain.MemberDto">
        SELECT m.user_id, phone, name, adddate, auth FROM member m JOIN authority au ON m.user_id = au.user_id
    </select>
    
    <insert id="insertMember">
        INSERT INTO member (user_id, password, name, phone, adduser)
        VALUES (#{user_id}, #{password}, #{name}, #{phone}, #{adduser})
    </insert>
    
    <insert id="insertAutho">
        INSERT INTO authority (user_id, auth)
        VALUES (#{user_id}, "팀원")
    </insert>

</mapper>